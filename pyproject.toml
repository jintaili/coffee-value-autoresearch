[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "coffee-value-shared"
version = "0.1.0"
description = "Versioned coffee feature extraction contract"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["coffee_value*"]
